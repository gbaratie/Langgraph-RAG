# Clé API OpenAI pour le LLM et les embeddings du RAG (optionnel en dev)
# OPENAI_API_KEY=sk-...

# Chroma : répertoire de persistance des vecteurs (local: ./data/chroma, Render: /data/chroma)
# CHROMA_PERSIST_DIR=./data/chroma

# Optionnel : serveur Chroma distant (ex. Chroma Cloud)
# CHROMA_SERVER_URL=

# CORS : origines autorisées (séparées par des virgules)
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Origine GitHub Pages en production (ex. https://votreuser.github.io)
# GITHUB_PAGES_ORIGIN=https://votreuser.github.io

# Sécurité : n'accepter que les requêtes avec Origin/Referer autorisé (bloque curl, Postman, scripts)
# REQUIRE_ORIGIN_CHECK=true   # si GITHUB_PAGES_ORIGIN est défini, activé par défaut

# Optionnel : clé API que le front doit envoyer (header X-API-Key). Sur GitHub Pages la clé est
# visible dans le build ; utile pour rotation et rate limiting.
# FRONTEND_API_KEY=une-cle-secrete-longue
